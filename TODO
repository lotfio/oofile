
validate no parameters make them exceptions
/*
File::create($file, $mode = "r");
File::rename($old, $new);
File::copy($old, $new);
File::move($from, $to);
File::delete($file);

Config::add() // adding config file or dir
Config::get() // get config key or set default
*/
https://cweiske.de/tagebuch/php-auto-coloring-output.htm

if notice exception change filename